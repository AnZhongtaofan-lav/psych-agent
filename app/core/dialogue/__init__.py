# -*- coding: utf-8 -*-
"""
对话子包初始化：对话策略调度与回复生成。

调度核心（dialogue/strategy.py）：
    (风险等级 × 用户画像) → 回复路径
    L3 → 拦截器接管（覆盖一切）
    L2 → 关怀 + 建议就医（陪伴模式，仅模板）
    L1/L0 → 画像策略模板；LLM 增强仅在 L0/L1 且画像允许时启用

安全铁律：
    P3 画像（allow_llm_reply=False）永远不使用 LLM 生成回复。
"""
